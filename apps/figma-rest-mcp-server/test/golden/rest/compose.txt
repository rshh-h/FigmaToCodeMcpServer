Box(
    modifier = Modifier.background(Color(0xFF000000)).width(390.dp).height(844.dp)
) {
    Box() {
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 108.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFFFFFFFF)).width(390.dp).height(736.dp))
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 810.dp)
    ) {
        Box(
        modifier = Modifier.width(390.dp).height(34.dp)
    ) {
        Box()
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 0.dp)
    ) {
        Box(
        modifier = Modifier.width(390.dp).height(47.dp).wrapContentWidth().wrapContentHeight()
    ) {
        Box()
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 47.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFFFFFFFF)).width(390.dp).height(54.dp).wrapContentWidth().wrapContentHeight())
    },
    Box(
        modifier = Modifier.offset(x = 290.dp, y = 58.dp)
    ) {
        Box(
        modifier = Modifier.background(Color(0x0D161823)).width(84.dp).height(32.dp).clip(RoundedCornerShape(36.dp)).padding(horizontal = 12.dp, vertical = 5.50.dp)
    ) {
        Row(
        modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(10.dp, Arrangement.Center)).verticalAlignment(Alignment.CenterVertically)
    ) {
        Text(
            text = "取消多选",
            fontSize = 15.sp,
            fontWeight = FontWeight.Medium,
            color = Color(0xFF161823)
        )
    }
    }
    },
    Box(
        modifier = Modifier.offset(x = 16.dp, y = 62.dp)
    ) {
        Box(
        modifier = Modifier.width(24.dp).height(24.dp).wrapContentWidth().wrapContentHeight()
    ) {
        Box() {
        Box(
            modifier = Modifier.offset(x = 0.dp, y = 0.dp)
        ) {
            Spacer(modifier = Modifier.background(Color(0x00FFFFFF)).width(24.dp).height(24.dp).wrapContentWidth().wrapContentHeight())
        },
        Box(
            modifier = Modifier.offset(x = 10.59.dp, y = 4.93.dp)
        ) {
            Spacer(modifier = Modifier.background(Color(0xFF161823)).width(12.dp).height(8.dp).wrapContentWidth().wrapContentHeight().clip(RoundedCornerShape(1.dp)))
        },
        Box(
            modifier = Modifier.offset(x = 19.07.dp, y = 10.59.dp)
        ) {
            Spacer(modifier = Modifier.background(Color(0xFF161823)).width(12.dp).height(8.dp).wrapContentWidth().wrapContentHeight().clip(RoundedCornerShape(1.dp)))
        }
    }
    }
    },
    Box(
        modifier = Modifier.offset(x = 150.dp, y = 62.dp)
    ) {
        Text(
        maxLines = 1,
        overflow = TextOverflow.Ellipsis,
        text = "所有照片",
        fontSize = 17.sp,
        fontWeight = FontWeight.Medium,
        color = Color(0xFF161823)
    )
    },
    Box(
        modifier = Modifier.offset(x = 223.11.dp, y = 65.37.dp)
    ) {
        Box(
        modifier = Modifier.width(16.25.dp).height(16.29.dp).wrapContentWidth().wrapContentHeight()
    ) {
        Box()
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 101.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFFFFFFFF)).width(390.dp).height(40.dp))
    },
    Box(
        modifier = Modifier.offset(x = 16.dp, y = 139.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFF161823)).width(120.dp).height(2.dp).wrapContentWidth().wrapContentHeight())
    },
    Box(
        modifier = Modifier.offset(x = 16.dp, y = 110.dp)
    ) {
        Text(
        text = "全部",
        fontSize = 15.sp,
        fontWeight = FontWeight.Medium,
        color = Color(0xFF161823),
        textAlign = TextAlign.Center
    )
    },
    Box(
        modifier = Modifier.offset(x = 254.dp, y = 110.dp)
    ) {
        Box(
        modifier = Modifier.alpha(0.60f)
    ) {
        Text(
        text = "图片",
        fontSize = 15.sp,
        fontWeight = FontWeight.Normal,
        color = Color(0xFF161823),
        textAlign = TextAlign.Center
    )
    }
    },
    Box(
        modifier = Modifier.offset(x = 135.dp, y = 110.dp)
    ) {
        Box(
        modifier = Modifier.alpha(0.60f)
    ) {
        Text(
        text = "视频",
        fontSize = 15.sp,
        fontWeight = FontWeight.Normal,
        color = Color(0xFF161823),
        textAlign = TextAlign.Center
    )
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 101.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFFFFFFFF)).width(390.dp).height(40.dp))
    },
    Box(
        modifier = Modifier.offset(x = 93.dp, y = 138.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0xFF161823)).width(68.dp).height(2.dp).wrapContentWidth().wrapContentHeight())
    },
    Box(
        modifier = Modifier.offset(x = 25.dp, y = 110.dp)
    ) {
        Box(
        modifier = Modifier.width(340.dp).height(21.dp)
    ) {
        Row(
        modifier = Modifier.horizontalArrangement(Arrangement.Center).verticalAlignment(Alignment.CenterVertically)
    ) {
        Box(
            modifier = Modifier.alpha(0.60f)
        ) {
            Text(
            text = "收藏",
            fontSize = 15.sp,
            fontWeight = FontWeight.Normal,
            color = Color(0xFF161823),
            textAlign = TextAlign.Center
        )
        },
        Text(
            text = "全部",
            fontSize = 15.sp,
            fontWeight = FontWeight.Medium,
            color = Color(0xFF161823),
            textAlign = TextAlign.Center
        ),
        Box(
            modifier = Modifier.alpha(0.60f)
        ) {
            Text(
            text = "视频",
            fontSize = 15.sp,
            fontWeight = FontWeight.Normal,
            color = Color(0xFF161823),
            textAlign = TextAlign.Center
        )
        },
        Box(
            modifier = Modifier.alpha(0.60f)
        ) {
            Text(
            text = "图片",
            fontSize = 15.sp,
            fontWeight = FontWeight.Normal,
            color = Color(0xFF161823),
            textAlign = TextAlign.Center
        )
        },
        Box(
            modifier = Modifier.alpha(0.60f)
        ) {
            Text(
            text = "动图",
            fontSize = 15.sp,
            fontWeight = FontWeight.Normal,
            color = Color(0xFF161823),
            textAlign = TextAlign.Center
        )
        }
    }
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 141.dp)
    ) {
        Box(
        modifier = Modifier.background(Color(0xFFFFFFFF)).width(390.dp).height(653.dp)
    ) {
        Column(
        modifier = Modifier.verticalArrangement(Arrangement.spacedBy(2.dp, Arrangement.Start)).horizontalAlignment(Alignment.Start)
    ) {
        Box(
            modifier = Modifier.fillMaxWidth().height(129.dp)
        ) {
            Row(
            modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(2.dp, Arrangement.SpaceBetween)).verticalAlignment(Alignment.Top)
        ) {
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            }
        }
        },
        Box(
            modifier = Modifier.fillMaxWidth().height(129.dp)
        ) {
            Row(
            modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(2.dp, Arrangement.SpaceBetween)).verticalAlignment(Alignment.Top)
        ) {
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            }
        }
        },
        Box(
            modifier = Modifier.fillMaxWidth().height(129.dp)
        ) {
            Row(
            modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(2.dp, Arrangement.SpaceBetween)).verticalAlignment(Alignment.Top)
        ) {
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box() {
                    Box(
                        modifier = Modifier.offset(x = 0.dp, y = 101.dp)
                    ) {
                        Box(
                        modifier = Modifier.background(Brush.linearGradient(
                            listOf(0f to Color(0xFF000000), 1f to Color(0xFF000000))
                        )).width(129.dp).height(28.dp).wrapContentWidth().wrapContentHeight().padding(horizontal = 8.dp, vertical = 6.dp)
                    ) {
                        Box() {
                        Box(
                            modifier = Modifier.offset(x = 6.dp, y = 6.dp)
                        ) {
                            Box(
                            modifier = Modifier.width(16.dp).height(16.dp)
                        ) {
                            Box()
                        }
                        },
                        Text(
                            text = "0:30",
                            fontSize = 13.sp,
                            fontWeight = FontWeight.Medium,
                            color = Color(0xFFFFFFFF),
                            textAlign = TextAlign.Right
                        )
                    }
                    }
                    }
                }
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            }
        }
        },
        Box(
            modifier = Modifier.fillMaxWidth().height(129.dp)
        ) {
            Row(
            modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(2.dp, Arrangement.SpaceBetween)).verticalAlignment(Alignment.Top)
        ) {
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box() {
                    Box(
                        modifier = Modifier.offset(x = 0.dp, y = 101.dp)
                    ) {
                        Box(
                        modifier = Modifier.background(Brush.linearGradient(
                            listOf(0f to Color(0xFF000000), 1f to Color(0xFF000000))
                        )).width(129.dp).height(28.dp).wrapContentWidth().wrapContentHeight().padding(horizontal = 8.dp, vertical = 6.dp)
                    ) {
                        Box() {
                        Box(
                            modifier = Modifier.offset(x = 6.dp, y = 6.dp)
                        ) {
                            Box(
                            modifier = Modifier.width(16.dp).height(16.dp)
                        ) {
                            Box()
                        }
                        },
                        Text(
                            text = "0:30",
                            fontSize = 13.sp,
                            fontWeight = FontWeight.Medium,
                            color = Color(0xFFFFFFFF),
                            textAlign = TextAlign.Right
                        )
                    }
                    }
                    }
                }
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box() {
                    Box(
                        modifier = Modifier.offset(x = 0.dp, y = 101.dp)
                    ) {
                        Box(
                        modifier = Modifier.background(Brush.linearGradient(
                            listOf(0f to Color(0xFF000000), 1f to Color(0xFF000000))
                        )).width(129.dp).height(28.dp).wrapContentWidth().wrapContentHeight().padding(horizontal = 8.dp, vertical = 6.dp)
                    ) {
                        Box() {
                        Box(
                            modifier = Modifier.offset(x = 6.dp, y = 6.dp)
                        ) {
                            Box(
                            modifier = Modifier.width(16.dp).height(16.dp)
                        ) {
                            Box()
                        }
                        },
                        Text(
                            text = "0:30",
                            fontSize = 13.sp,
                            fontWeight = FontWeight.Medium,
                            color = Color(0xFFFFFFFF),
                            textAlign = TextAlign.Right
                        )
                    }
                    }
                    }
                }
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            }
        }
        },
        Box(
            modifier = Modifier.fillMaxWidth().height(129.dp)
        ) {
            Row(
            modifier = Modifier.horizontalArrangement(Arrangement.spacedBy(2.dp, Arrangement.Start)).verticalAlignment(Alignment.Top)
        ) {
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            },
            Box(
                modifier = Modifier.width(129.dp).height(129.dp)
            ) {
                Box() {
                Box(
                    modifier = Modifier.offset(x = 0.dp, y = 0.dp)
                ) {
                    Box(
                    modifier = Modifier.width(129.dp).height(129.dp)
                ) {
                    Box()
                }
                }
            }
            }
        }
        }
    }
    }
    },
    Box(
        modifier = Modifier.offset(x = 231.5.dp, y = 639.dp)
    ) {
        Box(
        modifier = Modifier.width(20.dp).height(20.dp)
    ) {
        Box() {
        Box(
            modifier = Modifier.offset(x = 0.dp, y = 0.dp)
        ) {
            Box(
            modifier = Modifier.width(20.dp).height(20.dp)
        ) {
            Box() {
            Box(
                modifier = Modifier.offset(x = -1.dp, y = -1.dp)
            ) {
                Box(
                modifier = Modifier.width(22.dp).height(22.dp)
            ) {
                Box()
            }
            }
        }
        }
        }
    }
    }
    },
    Box(
        modifier = Modifier.offset(x = 0.dp, y = 141.dp)
    ) {
        Spacer(modifier = Modifier.background(Color(0x0D161823)).width(390.dp).height(48.dp))
    },
    Box(
        modifier = Modifier.offset(x = 48.dp, y = 156.dp)
    ) {
        Text(
        text = "往年今日 2018 年 7 月 25 日",
        fontSize = 13.sp,
        fontWeight = FontWeight.Medium,
        color = Color(0xFF161823)
    )
    },
    Box(
        modifier = Modifier.offset(x = 366.dp, y = 157.dp)
    ) {
        Box(
        modifier = Modifier.width(16.dp).height(16.dp)
    ) {
        Box()
    }
    },
    Box(
        modifier = Modifier.offset(x = 8.dp, y = 149.dp)
    ) {
        Spacer(modifier = Modifier.width(32.dp).height(32.dp).clip(RoundedCornerShape(4.dp)))
    }
}
}